(comment "Testing Nio Async Tests")

; (import-macros {: packadd!} :macros)
; (local {: describe : it} (require :plenary.busted))
; (let [nio (require :nio)
;       a nio.tests]
;   (describe :temp #(do
;                      #(it :returns {})
;                      #(it :false {})
;                      nil)))
